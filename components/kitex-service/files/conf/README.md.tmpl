# Configuration reference

**English** | [简体中文](README.zh-CN.md)

Generated by devkit (kitex-service@{{.ComponentVersion}}). **This file is managed:**
`devkit update` rewrites it, so it always describes the settings supported by
the version your service is on. Your `local.yaml`, `dev.yaml`, `uat.yaml`,
`prod.yaml` and any other `*.yaml` here are yours and are never rewritten; when
a new template version adds a setting, `devkit update` tells you, and you copy
it from here.

## How a file is chosen

| | |
|---|---|
| `APP_ENV` | selects `conf/<APP_ENV>.yaml`; default `local` |
| `CONF_DIR` | directory to read from. Otherwise `./conf`, then `conf` next to the executable |
| `${NAME}` | replaced by the environment variable `NAME` before parsing |

| Environment | File | Where | Nacos |
|---|---|---|---|
| `local` (no `APP_ENV`) | `local.yaml` | your own machine | the one on your machine; the service registers there |
| `dev` | `dev.yaml` | the shared development server | `${NACOS_ADDR}` from the deployment |
| `uat` | `uat.yaml` | user acceptance test | `${NACOS_ADDR}` |
| `prod` | `prod.yaml` | production | `${NACOS_ADDR}` |

A deployment always sets `APP_ENV` and a laptop never does, which is why no
`APP_ENV` means `local`. The three deployed files differ in little: addresses
and passwords come from the deployment (`NACOS_ADDR`, `NACOS_NAMESPACE`,
`NACOS_USERNAME`, `NACOS_PASSWORD`, `ADVERTISE_ADDR`), so one image goes from
dev to uat to prod unchanged.

**Every environment uses Nacos, and a service that cannot reach it refuses to
start.** The message names the address and the reason. On your own machine that
means a Nacos has to run; `local.yaml` has the command.

**A laptop never registers in a Nacos that other people use.** In the `local`
environment a service registers in a Nacos on the same machine only
(127.0.0.1, localhost). Pointing `local.yaml` at the development server's Nacos
without `register: false` is refused at start: registered there, your machine
would receive a share of everybody's requests, with code that is half written
or standing on a breakpoint. With `register: false` the service finds the
others and reads its configuration there, and is not announced.

**The Docker image has no `local.yaml`.** The Dockerfile sets `APP_ENV=prod`
as the default and leaves `local.yaml` out of the image, so a container whose
`APP_ENV` got emptied refuses to start and names the files there are, instead
of running with a developer's settings: a Nacos on 127.0.0.1, debug level,
console output.

To add an environment, copy `prod.yaml` to e.g. `staging.yaml` and start the
service with `APP_ENV=staging`.

## Every setting

```yaml
service:
  name: {{.Service}}          # registered name; callers use it
  addr: {{.Port}}              # port to listen on (all interfaces); "host:port" for one interface
  advertise: "${ADVERTISE_ADDR}"  # address callers reach this instance at, "host" or "host:port":
                              # a container behind the host's address and a mapped port.
                              # Empty: the address this machine reaches Nacos from

registry_disabled: false      # true = run without Nacos (tests, working offline): not registered,
                              # no discovery, configurations are files in conf/nacos/

nacos:
  addrs: ["127.0.0.1:8848"]   # Nacos servers host:port, several for a cluster
  namespace: ""               # namespace ID, "" = public
  group: DEFAULT_GROUP        # of the service and its configurations; callers must use the same
  username: ""                # only with Nacos auth enabled
  password: ""
  register: true              # false = find services and read configuration, do NOT announce
                              # this one: a laptop against the development server's Nacos
  watch_services: true        # log which services are alive: at start, and whenever one comes,
                              # goes or changes its number of instances; false = off
  sdk_log_level: warn         # level from which the Nacos client's own records are written (logger=nacos-sdk)
  cache_dir: /tmp/nacos/cache # Nacos client cache

config_data_id: ""            # Nacos data id read by kitexx.WatchConfig; "" = <service.name>.yaml

log:
  level: info                 # debug | info | warn | error
  format: console             # console | json (one JSON object per line)
  stacktrace: ""              # level from which records carry the call stack, e.g. error
  max_field_bytes: 0          # cut longer messages and field values; 0 = no limit
  sampling:                   # flood protection, off unless first is set
    first: 0                  # records per second and message written in full
    thereafter: 0             # after that every n-th
  buffer:                     # buffered output, off unless size is set
    size: 0                   # bytes
    flush_interval: 1s
  version: ""                 # "version" of every JSON record; default: VCS revision of the build

log_level_data_id: ""         # Nacos data id whose content is the log level; "" = off

shutdown:
  deregister_wait: 3s         # keep serving this long after leaving Nacos; 0s = off
  drain_timeout: 15s          # time for in-progress requests to finish
```

| Setting | Default when omitted |
|---------|----------------------|
| `service.name` | none, required |
| `service.addr` | `8888` |
| `service.advertise` | the address this machine reaches Nacos from, and the listen port |
| `registry_disabled` | `false` |
| `nacos.addrs` | none; required unless `registry_disabled: true` |
| `nacos.group` | `DEFAULT_GROUP` |
| `nacos.register` | `true`; `false` = find services and read configuration, do not announce this one |
| `nacos.watch_services` | `true` |
| `nacos.sdk_log_level` | `warn` |
| `nacos.cache_dir` | `/tmp/nacos/cache` |
| `config_data_id` | `<service.name>.yaml` |
| `log.level` | `info` |
| `log.format` | `console` (`json: true` of older services is still understood) |
| `log.stacktrace` | none |
| `log.max_field_bytes` | `0`, no limit |
| `log.sampling` | off |
| `log.buffer` | off |
| `log_level_data_id` | none |
| `shutdown.deregister_wait` | `3s` |
| `shutdown.drain_timeout` | `15s` |

## Which services are alive

Every service logs the state of all services in its Nacos namespace and group.
At start there is one record, `services alive`. After that `service online`,
`service offline` and `service instances changed` say what came or went and,
again, everything that is alive with its number of instances, so the last of
these records always tells how things are:

```
INFO service online name=ser-auth alive=1 services=2 instances=3
    overview:
      ┌──────────┬───┬──────────────────────────────────┐
      │ SERVICE  │ N │ INSTANCES                        │
      ├──────────┼───┼──────────────────────────────────┤
      │ ser-auth │ 1 │ + 127.0.0.1:8889                 │
      │ ser-user │ 2 │   127.0.0.1:8888  ← this process │
      │          │   │   127.0.0.1:8890                 │
      └──────────┴───┴──────────────────────────────────┘
```

`+` came (green), `-` went (red; a service of which nothing is left keeps a row
with N = 0), `~` is not what it was (yellow), and the arrow is the process that
prints the record.

An instance of a service that is known shows up within a second; the first
instance of a service whose name is new takes up to 3 seconds. In JSON the
same is `overview: {service, changed, alive}`. `nacos.watch_services: false` turns the
records off.

## Logging: the three production safeguards

`sampling`, `buffer` and `max_field_bytes` are off unless you set them, because
each of them changes what ends up in the log: one leaves records out, one
delays them, one cuts them. The first record of a starting service, `logger
configured`, shows which are in effect. `local.yaml` sets none of them, since in
development you want every record, at once and in full.

| | What it does | What it costs | `prod.yaml` |
|---|---|---|---|
| `sampling` | within a second, identical records are written `first` times, then every `thereafter`-th | records are left out; how many is reported | **on** |
| `max_field_bytes` | cuts a longer message or field value and says how long it was | the rest of that value is gone | **on**, 8192 |
| `buffer` | collects output in memory and writes it in larger pieces | the last `flush_interval` is lost when the process dies abnormally | **off** |

### sampling

It protects the disk and the log collector from a flood: when Redis is down,
every request logs the same error, tens of thousands of times a second, and
they all say the same. Records count as identical when level and **message**
are the same; fields do not count. So `zlog.Error("redis down", zlog.Int("uid",
uid))` is sampled as one message, while `zlog.Errorf("redis down for %d", uid)`
is a different message each time and is hardly sampled at all: one more reason
to put values into fields.

Nothing is dropped without a trace. For every second with drops there is one
record per level and message, at the level of what was dropped:

```
ERROR zlog: records dropped by sampling dropped_msg="redis down" dropped=4213
```

### max_field_bytes

A record is one line. Container runtimes split lines of more than 16 KB, and a
collector that gets the pieces no longer sees a JSON object: the record is
lost. The usual cause is `zlog.Any("resp", resp)` with a large value. The limit
is **per value**, not per record, which is why the template sets 8192 and not
16000: it leaves room for the other fields, but a record with several values
near the limit can still exceed 16 KB. Stack traces are not cut.

### buffer: when to turn it on

**Leave it off unless both of these are true:**

1. The service logs a lot, roughly **tens of thousands of records a second**,
   and a CPU profile shows writing the log as a real share of the CPU.
2. You accept losing the last moment of the log when the process dies
   abnormally.

Why the bar is that high: buffering saves one system call per record, about
**1 to 2 microseconds** (measured with stdout as a pipe, as in a container).

| Records per second | CPU spent writing, unbuffered | Saved by buffering |
|---|---|---|
| 1 000 (most services) | about 0.3 % of one core | about 0.2 % |
| 10 000 | about 3 % | about 2 % |
| 100 000 (gateways, battle servers) | about 27 % | about 19 %: now it pays |

What it costs. Records wait in memory for up to `flush_interval`.

- **Not lost**: a normal stop (SIGTERM, Ctrl+C; `kitexx.Run` flushes before it
  returns), a `zlog.Fatal`, a `zlog.Sync()`.
- **Lost**: whatever was still in memory when the process is killed for using
  too much memory, by `kill -9` or the platform's kill timeout, or by a panic
  or a fatal runtime error nobody recovered.

The second list is exactly when the last second of the log is what you need: it
says what the process was doing when it died. That is why the template turns
`sampling` on and `buffer` off, although both are for production: sampling only
acts in an emergency and tells you what it left out, whereas buffering is a
performance optimization that costs a little all the time and shows its price
at the worst moment.

When you do turn it on, use a short interval. Under a high volume the buffer
is flushed because it is full, not because of the timer, so a short interval
costs almost nothing and bounds what can be lost:

```yaml
log:
  buffer:
    size: 262144          # 256 KB
    flush_interval: 200ms # at most 0.2 s of records can be lost
```

## Configuration that follows Nacos

What is in `conf/<env>.yaml` is read once, at start. What should change while
the service runs (a level cap, a switch, a rate) goes into a configuration in
Nacos instead, and into a struct of its own in `app/app.go`:

```go
// app/app.go
type Dynamic struct {
	Battle struct {
		MaxLevel int `yaml:"max_level"`
	} `yaml:"battle"`
}

var Dyn *nacosx.Value[Dynamic]

func Setup(cfg *Config) (*handler.{{.Service | title}}ServiceImpl, error) {
	dyn, err := kitexx.WatchConfig[Dynamic](cfg.Config) // data id: config_data_id, default {{.Service}}.yaml
	if err != nil {
		return nil, err
	}
	Dyn = dyn
	return handler.New(), nil
}
```

```go
if level > app.Dyn.Get().Battle.MaxLevel { // always the version in effect
```

- A change published in the Nacos console is in effect a moment later. `Get`
  costs one atomic load and what it returned is never written to again, so no
  lock is needed. `Dyn.OnChange(func(old, cur *Dynamic) { ... })` is for what
  has to be rebuilt rather than read (a rate limiter, a pool).
- A change that does not parse, or that `func (d *Dynamic) Validate() error`
  (optional) refuses, is rejected with an ERROR record and the previous version
  stays in effect; so does the last version when the configuration is deleted.
  At start the same problems stop the service.
- The log says what changed, key by key (`~ battle.max_level  60 → 80`), hides
  the values of keys that look like secrets, and points out keys that `Dynamic`
  has no field for, which is what a typo looks like.
- Put into `Dynamic` only what may change at run time. A listen address or a
  database address there would look as if changing it did something.
- With `registry_disabled: true` there is no Nacos: the configuration is the
  file `conf/nacos/{{.Service}}.yaml`, and saving it counts as a change, so the
  above can be tried without a Nacos. `log_level_data_id` works the same way.

## Graceful stop

On SIGINT or SIGTERM the service: (1) deregisters from Nacos, (2) keeps serving
for `deregister_wait` so callers refresh their instance lists, (3) closes the
listener and lets requests in progress finish for up to `drain_timeout`,
(4) runs the `kitexx.OnShutdown` hooks. Keep `deregister_wait + drain_timeout`
below the platform's kill timeout (Kubernetes: 30s by default).

## Your own settings

Add a section to the YAML and a field to `Config` in **`app/app.go`**. That
file is yours; `cmd/{{.Service}}/main.go` belongs to the framework and must not
be edited, otherwise it can no longer be updated.

```go
// app/app.go
type Config struct {
	kitexx.Config `yaml:",inline"`
	Redis redisx.Config `yaml:"redis"`
}
```

```yaml
redis:
  addr: "${REDIS_ADDR}"
  password: "${REDIS_PASSWORD}"
```

Then create the dependency in `app.Setup` and register its cleanup there:

```go
// app/app.go
func Setup(cfg *Config) (*handler.{{.Service | title}}ServiceImpl, error) {
	rdb, err := redisx.New(context.Background(), cfg.Redis)
	if err != nil {
		return nil, err
	}
	kitexx.OnShutdown("redis", rdb.Close)
	return handler.New(rdb), nil
}
```
