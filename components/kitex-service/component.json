{
  "name": "kitex-service",
  "version": "0.8.4",
  "description": "Complete Kitex (Thrift) service project: Nacos registration, logging, config, Makefile codegen, CI for GitHub Actions and/or GitLab CI, Dockerfile. Used by `devkit ngs`.",
  "deps": [],
  "vars": [
    {
      "name": "Service",
      "description": "Service name, lowercase with dashes (e.g. order, order-item)",
      "required": true
    },
    {
      "name": "Module",
      "description": "Go module path of the new service (e.g. github.com/sezznaw/order)",
      "required": true
    },
    {
      "name": "CommonModule",
      "description": "Module path of the shared common library. Decided by the template for every service; cannot be set per service or project",
      "default": "github.com/sezznaw/devkit-common",
      "track": true
    },
    {
      "name": "CommonVersion",
      "description": "Version of common to require. Decided by the template for every service; cannot be set per service or project",
      "default": "v0.6.1",
      "track": true
    },
    {
      "name": "IdlRepo",
      "description": "Path of the shared IDL project as CI sees it (owner/repo on GitHub, group/project on GitLab). Empty: <owner or group of the service>/idl",
      "default": ""
    },
    {
      "name": "CI",
      "description": "Which CI configuration to generate: github, gitlab, both or none. ngs detects it from module_prefix / idl_repo",
      "default": "both"
    },
    {
      "name": "GoPrivate",
      "description": "GOPRIVATE pattern when the organisation's modules are private (empty = public)",
      "default": ""
    },
    {
      "name": "Port",
      "description": "Listen port",
      "default": "8888"
    },
    {
      "name": "NacosAddr",
      "description": "Address of the Nacos on a developer's own machine, written to conf/local.yaml",
      "default": "127.0.0.1:8848"
    },
    {
      "name": "GoVersion",
      "description": "Go release (major.minor) for go.mod, the Docker image and GitLab CI; must not be lower than what devkit-common requires. Decided by the template for every service; cannot be set per service or project",
      "default": "1.26",
      "track": true
    },
    {
      "name": "KitexVersion",
      "description": "Kitex library and tool version. Decided by the template for every service; cannot be set per service or project",
      "default": "v0.16.3",
      "track": true
    },
    {
      "name": "ThriftgoVersion",
      "description": "thriftgo tool version. Decided by the template for every service; cannot be set per service or project",
      "default": "v0.4.5",
      "track": true
    }
  ],
  "hooks": {
    "post_install": [
      "if command -v kitex >/dev/null 2>&1 && command -v thriftgo >/dev/null 2>&1; then make gen && go mod tidy; else echo '>> kitex/thriftgo not installed. Run: make tools && make gen && go mod tidy'; fi"
    ],
    "post_update": [
      "go get github.com/cloudwego/kitex@v0.16.3 github.com/sezznaw/devkit-common@v0.6.1 && if grep -rqs --include='*.go' --exclude-dir=kitex_gen 'github.com/sezznaw/devkit-common/log\"' .; then echo '>> This service still imports devkit-common/log, which devkit-common v0.3.0 replaced by zlog.'; echo '>> go mod tidy was skipped, it cannot succeed until that code is changed.'; echo '>> Change it as described below, then run: go mod tidy'; else go mod tidy; fi"
    ]
  },
  "once": [
    "go.mod",
    "idl.mk",
    "app/*",
    "README.md",
    "conf/*.yaml",
    "handler/*"
  ],
  "changelog": [
    {
      "version": "0.8.4",
      "changes": [
        "devkit-common v0.6.1: shorter console lines. Records of the framework used to start with an absolute path, because the common library (common/ next to the service) and Kitex (the module cache) are outside the service's directory, and the message ended up off the screen. Now it is `common/kitexx/kitexx.go:185` and `github.com/cloudwego/kitex@v0.16.3/pkg/remote/remotesvr/server.go:79`. Your own code stays `handler/handler.go:32`; JSON logs are unchanged"
      ]
    },
    {
      "version": "0.8.3",
      "changes": [
        "devkit-common v0.6.0: every service logs which services are alive. At start one record, \"services alive\", lists every service with its number of instances and their addresses. After that \"service online\", \"service offline\" and \"service instances changed\" say what came or went and, again, everything that is alive, so when ser-auth starts, ser-user says so and shows the whole picture. It also sees the last instance of a service go, which the Nacos client that calls go through never hears of",
        "with these records on, \"service discovered\" and \"instances changed\" of the services this one calls repeat them and move to debug; the warning that nothing can take the requests of a called service stays"
      ],
      "action": [
        "Nothing has to change: the records are on. To turn them off in one environment, add to the nacos section of that conf/*.yaml:\n  watch_services: false"
      ]
    },
    {
      "version": "0.8.2",
      "changes": [
        "a service whose name begins with \"new\" (news, new-order) compiles. thriftgo renames a Thrift service that begins with \"New\", because it would clash with the NewXxx constructors it generates: NewsService becomes package newsservice_. main.go imported newsservice, so such a service did not build after `devkit ngs`. main.go now follows that rule and refers to the package by a fixed alias",
        "a start-up problem is logged from the step that failed. main.go called zlog.Fatal through a helper at the end of the file, so every \"cannot start\" record pointed at the same line of that helper, whether the configuration, the framework or app.Setup had failed. The helper is gone and the record names the line of the step"
      ]
    },
    {
      "version": "0.8.1",
      "changes": [
        "English and Chinese no longer run into each other. conf/README.md is English only and conf/README.zh-CN.md is the same document in Chinese, with a link to switch at the top of each; before, both languages shared lines, tables and YAML comments and neither could be read",
        "new services get conf/*.yaml whose comments keep the languages apart: the English block, an empty comment line, then the Chinese block, and a file header that is English first and Chinese second. Nothing about the settings changed"
      ],
      "action": [
        "Your conf/*.yaml are yours and keep their comments as they are. If you want the new layout, the quickest way is to generate a throwaway service with `devkit ngs` and copy the comments over; the settings are the same."
      ]
    },
    {
      "version": "0.8.0",
      "changes": [
        "four environments, one file each: conf/local.yaml (your own machine; what a service uses when APP_ENV is not set), conf/dev.yaml (the shared development server), conf/uat.yaml and conf/prod.yaml. \"dev\" used to mean the laptop, which left no name for the development server. `make run` uses local; the Docker image contains every environment except local",
        "every environment uses Nacos, the laptop too: new services get a conf/local.yaml that registers in the Nacos on 127.0.0.1:8848 and refuses to start when it is not running. The file has the docker command, and reads the account from NACOS_USERNAME and NACOS_PASSWORD",
        "devkit-common v0.5.0: a laptop never registers in a Nacos that other people use. In the local environment a service registers in a Nacos on the same machine only; anything else is refused at start. `nacos.register: false` is the way to work from your machine against the development server's Nacos: the service finds the others and reads its configuration there, and is not announced",
        "devkit-common v0.5.0: what gets registered is the address this machine reaches Nacos from (the right one with a VPN or Docker networks; 127.0.0.1 with a local Nacos). `service.advertise` (\"host\" or \"host:port\", from ADVERTISE_ADDR in the deployed files) is registered instead, for containers that are reached through the host's address and a mapped port"
      ],
      "action": [
        "The default environment is now local, so a service without conf/local.yaml no longer starts on your machine. Your old conf/dev.yaml was the laptop file; make it that again, and give the servers theirs:\ngit mv conf/dev.yaml conf/local.yaml\ncp conf/prod.yaml conf/dev.yaml     # then set   level: debug\ncp conf/prod.yaml conf/uat.yaml",
        "In conf/local.yaml set `registry_disabled: false` and the account of your Nacos (conf/README.md has every setting):\nnacos:\n  addrs: [\"127.0.0.1:8848\"]\n  username: \"${NACOS_USERNAME}\"\n  password: \"${NACOS_PASSWORD}\"",
        "In conf/dev.yaml, conf/uat.yaml and conf/prod.yaml add below `addr:` (only matters for containers behind a mapped port):\n  advertise: \"${ADVERTISE_ADDR}\""
      ]
    },
    {
      "version": "0.7.0",
      "changes": [
        "devkit-common v0.4.0: configuration that follows Nacos while the service runs. `kitexx.WatchConfig[Dynamic](cfg.Config)` reads the Nacos configuration `<service.name>.yaml` (setting `config_data_id`) into a struct and keeps it current: `Get()` is always the version in effect. A change that does not parse or validate is rejected and the previous version stays; the log lists what changed key by key, with secrets hidden, and points out keys with a typo. conf/README.md has a new section on it",
        "registration and discovery are on record: `registered in Nacos`, `service discovered` and `instances changed` with a line per instance (`+ 10.0.0.7:8888  weight=10`, `- 10.0.0.6:8888`), `deregistered from Nacos`",
        "the Nacos client logs through the logger of the service (`logger=nacos-sdk`, new setting `nacos.sdk_log_level`, default warn) instead of into /tmp/nacos/log/nacos-sdk.log. While Nacos is unreachable its reconnect messages, several a second, are replaced by three records: `nacos connection lost`, `nacos is still unreachable`, `nacos connection restored`",
        "`registry_disabled: true` now means: run without Nacos. In addition to not registering, a configuration read with WatchConfig or `log_level_data_id` is the file `conf/nacos/<data id>`, and saving the file counts as a change",
        "services on this version and on earlier ones keep finding each other"
      ],
      "action": [
        "Nothing has to change. `nacos.log_dir` in your conf/*.yaml is ignored from now on and can be removed.",
        "Only if your own code called nacosx.SharedNamingClient, NewNamingClient or NewConfigClient: use `nc, err := kitexx.Nacos(cfg.Config)` and then nc.Naming() or nc.ConfigClient().",
        "To use the new configuration, see the section \"Configuration that follows Nacos\" in conf/README.md."
      ]
    },
    {
      "version": "0.6.6",
      "changes": [
        "devkit-common v0.3.4: `service.addr` takes a port. New services get `addr: 8888` in conf/dev.yaml and conf/prod.yaml instead of the quoted `\":8888\"`. A bare port listens on every network interface, exactly like `\":8888\"` did; `\"host:port\"` is still there for listening on one interface only",
        "a bad `service.addr` is reported with the name of the setting and what it accepts, instead of \"missing port in address\""
      ],
      "action": [
        "Nothing has to change: `addr: \":8888\"` in your conf/*.yaml keeps working. If you prefer the short form, replace the line with `addr: 8888` (your port) in conf/dev.yaml and conf/prod.yaml."
      ]
    },
    {
      "version": "0.6.5",
      "changes": [
        "devkit-common v0.3.3: log sampling says what it left out. Every second in which records were dropped ends with one record per level and message, \"zlog: records dropped by sampling\" with dropped_msg and dropped, so the size of a flood can be read from the log",
        "conf/README.md has a new section on the three production safeguards of the logger (sampling, max_field_bytes, buffer): what each protects against, what it costs, and when to turn buffer on, which prod.yaml leaves off on purpose"
      ]
    },
    {
      "version": "0.6.4",
      "changes": [
        "the Docker image no longer contains conf/dev.yaml. A container whose APP_ENV got lost or overridden used to start with the development settings and look healthy: not registered in Nacos, so no caller found it, and logging colored text that a collector cannot parse. Now it refuses to start and names the configuration files there are"
      ],
      "action": [
        "Only if you run the image with APP_ENV=dev somewhere, for example in a development cluster: that no longer starts. Give that environment a file of its own:\ncp conf/prod.yaml conf/test.yaml      # then adjust it, and deploy with APP_ENV=test",
        "If you had edited the Dockerfile, the new one is next to it as Dockerfile.new; take over the two lines that mention /out/conf."
      ]
    },
    {
      "version": "0.6.3",
      "changes": [
        "devkit-common v0.3.2: the \"logger configured\" record of a starting service says where the values come from that the framework fills in: `service: order (from service.name)`, `env: prod (from APP_ENV)`, or `env: dev (APP_ENV is not set)`. In JSON these remarks, and which settings are defaults, are in \"config_notes\""
      ]
    },
    {
      "version": "0.6.2",
      "changes": [
        "devkit-common v0.3.1: the first record of a starting service, \"logger configured\", lists the log settings in effect (level, format, stacktrace, sampling, buffer, max_field_bytes, service, env, version, host, output) under the names they have in conf/*.yaml, with \"(default)\" behind what was not set. It is written whatever the level",
        "a typo in the log settings (for example format: consol) is now reported whatever the level; with level: error the warning used to filter itself out"
      ]
    },
    {
      "version": "0.6.1",
      "changes": [
        "devkit update no longer stops with an error from `go mod tidy` when a service still uses the log package that devkit-common v0.3.0 removed: it skips that step, says why, and goes on to show the steps below. 0.6.0 stopped before it could show them"
      ],
      "action": [
        "If your code still imports \"<common>/log\", move it to zlog, then finish the update by hand:\nreplace the import \"<common>/log\" with \"<common>/zlog\"\nlog.FromContext(ctx).Info(\"ping\", \"message\", m)   ->   zlog.Ctx(ctx).Info(\"ping\", zlog.Str(\"message\", m))\ngo mod tidy && go build ./... && go vet ./..."
      ]
    },
    {
      "version": "0.6.0",
      "changes": [
        "devkit-common v0.3.0: new logger zlog (on zap) replaces the log package. Console output for people: a color per level, file:line that GoLand and terminals turn into a link, key=value fields, stacks of errors and panics below the record with every frame a link. JSON output for collectors: service, env, host, version and the trace_id of the request on every record, never the same key twice",
        "a trace_id follows a request from service to service (kitexx.ClientOptions now uses the TTHeader transport); zlog.Ctx(ctx) is the logger of the request",
        "what Kitex and dependencies log (klog, log, log/slog) comes out in the same format",
        "new log settings: format, stacktrace, max_field_bytes, sampling, buffer, version; log_level_data_id makes the level follow a Nacos configuration at run time",
        "a service that cannot read its configuration now reports it through the logger; the Dockerfile sets ZLOG_FORMAT=json so that this line is JSON too"
      ],
      "action": [
        "The log package is gone from devkit-common, so handler code has to move to zlog. Replace the import \"<common>/log\" with \"<common>/zlog\" and rewrite the calls:\nlog.FromContext(ctx).Info(\"ping\", \"message\", m)   ->   zlog.Ctx(ctx).Info(\"ping\", zlog.Str(\"message\", m))\nslog.Error(\"save failed\", \"err\", err)             ->   zlog.Error(\"save failed\", zlog.Err(err))\nFields: zlog.Str, Int, Float, Bool, Dur, Time, Err, Any, Secret. go vet ./... finds what is left.",
        "In conf/*.yaml replace `json: false` with `format: console` and `json: true` with `format: json`. The old key still works, so nothing breaks if you wait.",
        "Clients made with kitexx.ClientOptions now speak TTHeader. Kitex servers accept it without any change; only a server that is not Kitex needs a look."
      ]
    },
    {
      "version": "0.5.1",
      "changes": [
        "devkit-common v0.2.1: when Nacos cannot be reached the service stops at once and names the address, the reason and the setting to check, instead of \"client not connected, current status:STARTING\" after a few seconds; it also explains a closed gRPC port (main port + 1000)"
      ]
    },
    {
      "version": "0.5.0",
      "changes": [
        "one Kitex version for the whole team: devkit update now also aligns the Kitex library in go.mod with the template, not only the generator version in the Makefile",
        "make gen stops with a clear message when the installed kitex or thriftgo is not the team's version, instead of generating different code",
        "versions can no longer be chosen per service or per project (needs devkit 0.1.9 or newer)",
        "Dockerfile and GitLab CI use the Go release the common library needs (GoVersion, 1.26); with golang:1.24 the image refused to build because devkit-common requires Go 1.26"
      ],
      "action": [
        "If `make gen` now reports another kitex or thriftgo version, run:\nmake tools",
        "If devkit.yaml has KitexVersion, ThriftgoVersion or CommonVersion under `vars:`, remove those lines; devkit 0.1.9 refuses them."
      ]
    },
    {
      "version": "0.4.1",
      "changes": [
        "library and tool versions (KitexVersion, ThriftgoVersion, CommonVersion) now follow the template on update instead of staying at the value from when the service was created; needs devkit 0.1.8 or newer"
      ],
      "action": [
        "Only if you want a service to stay on a specific version, pin it:\ndevkit update --set KitexVersion=v0.16.3"
      ]
    },
    {
      "version": "0.4.0",
      "changes": [
        "new app/app.go, a file you own: the service's Config struct, a Setup function for its dependencies and shutdown cleanup, and optional extra server options",
        "cmd/<service>/main.go is now pure framework code that calls app.Setup; nothing service-specific lives in it any more, so it can always be updated"
      ],
      "action": [
        "If you had edited cmd/<service>/main.go, it was left alone and the new version is next to it as main.go.new. Move your changes into app/app.go, then take the new file:\nConfig fields            -> the Config struct in app/app.go\nkitexx.OnShutdown(...)   -> app.Setup\nextra server options     -> app.ServerOptions\nmv cmd/<service>/main.go.new cmd/<service>/main.go",
        "If your handler needs dependencies, create them in app.Setup and pass them to handler.New(...)."
      ]
    },
    {
      "version": "0.3.1",
      "changes": [
        "conf/README.md: a managed reference of every configuration setting, kept up to date by devkit update",
        "new services get conf/dev.yaml and conf/prod.yaml with a detailed English/Chinese comment on every setting"
      ],
      "action": [
        "Your existing conf/*.yaml were not touched. For the explanations, read conf/README.md,\nor create a scratch service with `devkit ngs` and copy the commented files from it."
      ]
    },
    {
      "version": "0.3.0",
      "changes": [
        "main.go: configuration is found from any start directory ($CONF_DIR, ./conf, next to the executable); start-up errors are plain messages instead of a panic",
        "graceful stop: keeps serving after leaving Nacos, configurable drain time, kitexx.OnShutdown hooks that run after the last request",
        "Kitex's internal logs use the same format as yours (logger=kitex); requires devkit-common v0.2.0, upgraded for you"
      ],
      "action": [
        "Optional, in each conf/<env>.yaml (defaults are 3s and 15s). Locally use deregister_wait: 0s so stopping is instant:\nshutdown:\n  deregister_wait: 3s\n  drain_timeout: 15s",
        "If the service owns resources, release them on shutdown. Since 0.4.0 this goes into app.Setup in app/app.go:\nkitexx.OnShutdown(\"db\", db.Close)"
      ]
    },
    {
      "version": "0.2.1",
      "changes": [
        "the list of IDLs to generate moved from the Makefile to idl.mk, a file you own, so the Makefile can keep being updated"
      ],
      "action": [
        "If you had added IDLs to the IDLS line of the Makefile, put them into idl.mk:\nIDLS := <service>/<service>.thrift other/other.thrift"
      ]
    },
    {
      "version": "0.2.0",
      "changes": [
        "CI for GitLab (.gitlab-ci.yml) next to GitHub Actions; which one is generated follows where the code is hosted"
      ],
      "action": [
        "If both files were generated and you only need one: devkit update --force --set CI=gitlab   (or github)"
      ]
    }
  ]
}
